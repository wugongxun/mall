server:
  port: 10000

spring:
  #数据库配置
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://124.222.77.187:3306/mall_pms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: wgx
  #redis配置
  redis:
    #上传服务器之前修改为localhost
    host: 124.222.77.187
    port: 6379
    password: wgx
    connect-timeout: 15000
    lettuce:
      pool:
        max-active: 8
        max-wait: 15000
        max-idle: 10
        min-idle: 0

#mybatis-plus配置
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto