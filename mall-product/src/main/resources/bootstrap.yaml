spring:
  cloud:
    nacos:
      config:
        name: mall-product
        server-addr: 192.168.154.128:8848
        file-extension: yaml
        namespace: product
        group: dev
        extension-configs:
          - data-id: datasource.yaml
            group: dev
          - data-id: mybatis-plus.yaml
            group: dev
