spring:
  cloud:
    nacos:
      config:
        server-addr: 192.168.154.128:8848
        name: mall-ware
        namespace: ware
        file-extension: yaml
        group: dev
        extension-configs:
          - data-id: datasource.yaml
            group: dev
          - data-id: mybatis-plus.yaml
            group: dev