spring:
  cloud:
    nacos:
      config:
        name: mall-other
        server-addr: 192.168.154.128:8848
        file-extension: yml
        namespace: other
        group: dev
        extension-configs:
          - data-id: aliyun.yml
            group: dev